<?php
session_start();
require_once 'db.php'; // Arquivo com a conexão ao banco

// Verifica se há livros cadastrados
$stmt = $pdo->query("SELECT * FROM livros ORDER BY id DESC");
$livros = $stmt->fetchAll();
?>

<!DOCTYPE html>
<html>
<head>
    <title>Plataforma de Livros</title>
</head>
<body>
    <h1>Meus Livros</h1>
    
    <?php if (empty($livros)): ?>
        <p>Nada para ver por aqui, faça seu primeiro upload e ele aparecerá aqui!</p>
    <?php else: ?>
        <?php foreach ($livros as $livro): ?>
            <div>
                <h2><?= htmlspecialchars($livro['titulo']) ?></h2>
                <p>Autor: <?= htmlspecialchars($livro['autor']) ?></p>
                <p><?= htmlspecialchars($livro['descricao']) ?></p>
                <img src="<?= htmlspecialchars($livro['capa']) ?>" alt="Capa do livro" width="100">
                <a href="<?= htmlspecialchars($livro['arquivo']) ?>">Baixar livro</a>
            </div>
        <?php endforeach; ?>
    <?php endif; ?>
    
    <a href="upload.php"><button>Upload</button></a>
</body>
</html>